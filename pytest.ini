[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Don't run slow integration tests by default (need Docker running)
addopts = --ignore=tests/integration -v --tb=short
